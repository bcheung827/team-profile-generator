const Intern = require("../lib/Intern");

describe("Turtle", () => {
    describe("getTurtles", () => {
        it("shold set", () => {
            const num = 2;
            const t = new Turtle(num);
            expect(t.num).toEqual(num);
        });

        it("shold set", () => {
            const num = 2;
            const expected = 
            const t = new Turtle(num);
            expect(t.num).toEqual(num);
        });
    })
})